class ExamResult {
    constructor(id, studentId, examId, grade) {
        this.id = id;
        this.studentId = studentId;
        this.examId = examId;
        this.grade = grade;
    }
}

export default ExamResult;
